provider_installation {
  filesystem_mirror {
    path    = ".terraform/mirror"
    include = ["terraform.local/skpr/*"]  # only your custom providers
  }

  # Optional: block direct downloads for those providers
  direct {
    exclude = ["terraform.local/skpr/*"]
  }
}
